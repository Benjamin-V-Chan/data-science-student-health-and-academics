# Load the cleaned dataset
# Encode categorical variables
# Split data into training and testing sets
# Train a simple classification model (e.g., Decision Tree) to predict "Health rating"
# Save the trained model and evaluation results to `outputs/`
